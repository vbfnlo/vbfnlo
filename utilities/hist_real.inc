* variables for real emission error estimation

      integer max_calls
      PARAMETER (max_calls = 10)    ! number of entrys that can be put into a histogram per phase space point;
                                    ! for example the example histogram with ID 1 calls FillHist twice per phase
                                    ! space point


